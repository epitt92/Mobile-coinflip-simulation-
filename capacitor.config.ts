import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starterv0',
  appName: 'coin-flip-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
